@page "/images/create"

@layout ImageLayout

@inject NavigationManager NavigationManager

<CreateImage OnSubmit="@(() => NavigationManager.NavigateTo("images"))"
             ReturnTitle="Index"
             ReturnUrl="images"
             Title="Create" />