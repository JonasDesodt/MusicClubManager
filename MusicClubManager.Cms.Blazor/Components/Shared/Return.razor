<span>
    <NavLink href="@Href">&larr; @Title</NavLink>
</span>

@code {
    [Parameter, EditorRequired]
    public required string Href { get; set; }

    [Parameter, EditorRequired]
    public required string Title { get; set; }
}
